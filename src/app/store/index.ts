// Fichier temporairement adapté en attendant l'installation correcte de NgRx

// import { isDevMode } from '@angular/core';
// import { ActionReducerMap, MetaReducer } from '@ngrx/store';

// Définition de l'état global de l'application
export interface AppState {
  // Ajouter ici les différents états de l'application
}

// Réducteurs de l'application - commentés en attendant l'installation de NgRx
// export const reducers: ActionReducerMap<AppState> = {
//   // Ajouter ici les réducteurs
// };

// MetaReducers pour le développement
// export const metaReducers: MetaReducer<AppState>[] = isDevMode() ? [] : [];

// Version temporaire pour permettre la compilation
export const reducers = {};
export const metaReducers = [];
